const Host = "localhost";
const port = "8080";
const url = Host+":"+port;

/* Perfiles */
const Perfiles = {
    ADMIN : "ADM",
    ASSESOR : "ASER",
    COORDINADOR: "COORD"
}

/* Data almacenar */
export  { url, Perfiles }